"""LaTeX parser → sections, equations, tables, refs. Skeleton — ships in Phase 2."""
from __future__ import annotations


class LaTeXParser:
    def parse(self, source: str) -> dict:
        raise NotImplementedError("LaTeXParser ships in Phase 2")
